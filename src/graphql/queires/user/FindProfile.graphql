query FindProfile {
    findProfile {
        id
        username
        displayName
        email
        avatar
        bio
        isTotpEnabled
        notificationSettings {
            siteNotifications
            telegramNotifications
        }
        stream {
            serverUrl
            streamKey
        }
    }
}
